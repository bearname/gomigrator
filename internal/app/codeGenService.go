package app

type CodeGenServiceImpl struct {
}

func (c *CodeGenServiceImpl) Generate(model string, outputPath string) error {
	//split := strings.Split(model, ";")
	//modelPath := split[0]
	//modelName := split[1]
	//
	return nil
}
